/** Billing product IDs used by react-native-iap (iOS/Android) and Stripe (web). */

export const PRO_ENTITLEMENT = 'pro';

export const PRODUCT_IDS = {
  monthly: 'monthly',
  yearly: 'yearly',
  lifetime: 'lifetime',
} as const;
